# ProofMind Frontend (Vite + React)

## Setup
```
npm install
cp .env.example .env
```

## Run
```
npm run dev
```

The app expects a backend at `VITE_API_BASE_URL` (default `http://localhost:8000`).
